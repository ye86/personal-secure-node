# Web文件管理与个人数据中心（v0.21）

## 使用方式

启动节点HTTPS服务后，在浏览器访问节点地址：

```text
https://127.0.0.1:7780/
```

通过设备挑战登录获取短期令牌：

```powershell
python drive.py device-login https://127.0.0.1:7780 `
  CERT_FINGERPRINT DEVICE_ID D:\PsnDevice\device.key
```

把返回的 `access_token` 粘贴到页面。令牌只保存在当前页面的JavaScript内存变量中，不写入Cookie、Local Storage或Session Storage；刷新或关闭页面后需要重新输入。

## 当前能力

- 显示节点连接状态；
- 显示活动文件数、版本数、物理占用和配额；
- 列出当前可见文件、大小和更新时间；
- 使用认证请求下载文件；
- 选择本地文件并分块上传到指定Vault路径；
- 查看历史版本并将历史版本恢复为新的当前版本；
- 使用设备签名的一次性动作令牌将文件移入回收站；
- 按逻辑目录逐层浏览；
- 移动或重命名文件而不复制内容；
- 查看回收站并撤销删除；
- 使用 `file.purge` 动作令牌永久清除全部版本。
- 手动刷新状态；
- 查看服务运行状态、节点地址、日志大小和存储摘要；
- 查看最近服务事件；
- 从Web页面触发启动预检；
- 从Web页面生成脱敏诊断包；
- 以普通用户能理解的方式显示“个人数据中心”首页；
- 将服务状态、日志、PID和预检等技术信息折叠到“高级诊断”；
- 创建、查看和撤销只读分享链接；
- 设置分享链接有效期和最多下载次数；
- 注册插件清单；
- 查看插件发布主体、作者主体和权限声明；
- 启用或禁用插件；
- 封禁或解除封禁插件发布主体。

上传和版本恢复使用普通 `drive:write` 短期令牌。删除需要额外的一次性管理员动作令牌，它绑定当前设备、`file.delete`动作和完整文件路径，默认5分钟失效且只能使用一次。

个人数据中心首页中的文件与空间信息使用 `drive:read` 令牌。创建或撤销分享链接使用 `drive:write` 令牌；公开下载链接不需要认证。插件列表使用 `drive:read` 令牌；注册插件、启用禁用插件和封禁主体使用 `drive:write` 令牌。高级诊断中的状态、事件和启动预检也使用 `drive:read` 令牌；生成诊断包使用 `drive:write` 令牌。诊断包仍由服务端的脱敏逻辑生成，不包含 `.psn/master.key`、`.psn/tls.key` 或加密Blob内容，但可能包含路径、节点URL、证书指纹和日志上下文。

## 分享链接

在文件列表中点击“分享”可创建公开只读下载链接。链接具有以下属性：

- 固定到创建时的文件版本；
- 可设置1至365天有效期；
- 可设置最多下载次数，也可以不限次数；
- 可在“分享链接”区域撤销；
- 文件被永久清除后，对应分享链接也会失效。

分享链接适合局域网、家庭网络、反向代理或你已经配置好的公网入口。v0.19不负责NAT穿透、动态域名、HTTPS公网证书申请或下载页美化。

## 插件中心

v0.20插件中心先做信任关系管理，不执行第三方代码。用户能看到：

- 插件或作品名称；
- 发布主体；
- 作者主体；
- 权限声明；
- 插件状态；
- 是否因为主体封禁而被阻止。

如果某个主体越界或不可信，用户可以封禁该发布主体及其子作品。这个能力会成为未来插件市场和AI Skills共用的权限底座。

生成删除动作令牌：

```powershell
python drive.py admin-authorize `
  https://127.0.0.1:7780 CERT_FINGERPRINT DEVICE_ID `
  D:\PsnDevice\device.key file.delete "documents/example.pdf"
```

把返回的 `action_token` 粘贴到删除对话框。即使浏览器短期访问令牌泄露，没有设备私钥签名也不能直接生成删除令牌。

永久清除使用不同动作，不能复用删除令牌：

```powershell
python drive.py admin-authorize `
  https://127.0.0.1:7780 CERT_FINGERPRINT DEVICE_ID `
  D:\PsnDevice\device.key file.purge "documents/example.pdf"
```

## 前端安全边界

- HTML、JavaScript和CSS均由节点本地提供，不加载CDN、字体或第三方统计；
- 禁止内联脚本和外部源，CSP仅允许同源资源及同源API；
- 页面禁止被其他网站嵌入框架；
- 禁用摄像头、麦克风和定位权限；
- API令牌不出现在URL中；
- 管理员动作令牌具有短期、单次、设备和资源绑定；
- 所有API响应及页面资源使用 `no-store`；
- 前端使用 `textContent` 构建文件列表，不把文件名作为HTML执行。
- 管理控制台暂不提供停止服务、改配置、删除诊断包等破坏性操作。

浏览器中的脚本仍可读取内存令牌，因此任何未来引入的第三方脚本、富文本预览或插件都会显著改变威胁模型，必须重新审查。

## 上传限制

Web上传使用4 MiB分块并在当前页面生命周期内复用会话键。网络中断后，在不刷新页面的情况下重新提交同一个文件可以继续；刷新页面后暂不保证找到旧会话，但服务端孤立会话会按过期策略清理。

浏览器无法安全读取其他应用私有文件，用户必须主动选择文件。大文件上传尚无暂停按钮、并行块策略和带宽限制。

## 尚未实现

- 设备签名直接登录浏览器；
- WebAuthn或操作系统通行密钥；
- 空目录的独立创建和保存；
- 批量移动、批量恢复和保留期限策略；
- 图片缩略图及安全媒体预览；
- 浏览器直接调用硬件密钥完成管理员签名；
- 自动刷新令牌和安全退出按钮；
- 控制台内启动/停止服务和配置编辑；
- 诊断包下载、删除和保留策略；
- 分享链接下载页美化、批量分享和上传收件箱；
- 插件安装、启用、禁用和权限授权；
- 插件代码执行、沙箱隔离和运行时权限拦截；
- 完整无障碍及移动设备测试。
